export interface NotifyData {
    message: string;
    title: string;
    buttonText: string
};

export interface ConfirmData{
    message: string;
    title: string;
    warnYes: boolean;
}

export const WIDTH = '350px';